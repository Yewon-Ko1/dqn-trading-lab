# 논문 수치를 생산한 실제 실행 환경 (2026-09-17 고정)
# requirements.txt 는 개발용 느슨한 범위, 이 파일은 재현용 정확한 버전이다.
# 강화학습은 부동소수점 차이 하나가 ε-greedy 선택을 뒤집어 궤적 전체를 바꾸므로
# 버전이 다르면 같은 시드라도 같은 숫자가 나오지 않는다 (decisions.md "실행 환경" 참조).
#
# Python 3.13.9 (MSC v.1944 64bit, Windows AMD64)

torch==2.12.0+cpu
numpy==2.5.2
pandas==2.3.3
